# Cloud Build configuration for HSS Backend (SAFE VERSION)
# Triggers: develop branch (dev), v* tags (prod)
# FIX: SERVICE_NAME artık _SERVICE_NAME olarak substitution; tüm bash değişkenleri MY_ prefix ile korunuyor.

steps:
  # 1. Maven dependencies download (optimized caching)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'maven-deps'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "📦 Maven dependencies indiriliyor..."
        echo "Mevcut dizin: $(pwd)"
        echo "Dizin içeriği: $(ls -la)"
        # Root dizinden hss-backend'e geç veya mevcut dizinde kal
        if [ -d "hss-backend" ]; then
          cd hss-backend
          echo "hss-backend dizinine geçildi"
        elif [ -f "pom.xml" ]; then
          echo "Mevcut dizinde pom.xml bulundu, burada kalıyoruz"
        else
          echo "❌ hss-backend dizini veya pom.xml bulunamadı!"
          echo "Mevcut dizin içeriği:"
          ls -la
          exit 1
        fi
        
        # Java 21 kurulumu (sadece bir kez)
        echo "☕ Java 21 kuruluyor..."
        apt-get update -qq && apt-get install -y -qq openjdk-21-jdk-headless
        export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
        export PATH=$$JAVA_HOME/bin:$$PATH
        java -version
        
        # Maven cache'ini optimize et
        mkdir -p /workspace/.m2/repository
        export MAVEN_OPTS="-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication"
        
        # Dependencies'leri indir ve cache'le
        ./mvnw -B dependency:go-offline \
          -Dmaven.repo.local=/workspace/.m2/repository \
          -Dmaven.test.skip=true \
          -Dmaven.javadoc.skip=true \
          -Dmaven.source.skip=true \
          -q
        
        # Cache'i koru
        echo "✅ Dependencies hazır ve cache'lenmiş!"

  # 2. Test aşaması (parallel with build)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'test'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "🧪 Backend testleri çalıştırılıyor..."
        echo "Mevcut dizin: $(pwd)"
        # Root dizinden hss-backend'e geç veya mevcut dizinde kal
        if [ -d "hss-backend" ]; then
          cd hss-backend
          echo "hss-backend dizinine geçildi"
        elif [ -f "pom.xml" ]; then
          echo "Mevcut dizinde pom.xml bulundu, burada kalıyoruz"
        else
          echo "❌ hss-backend dizini veya pom.xml bulunamadı!"
          echo "Mevcut dizin içeriği:"
          ls -la
          exit 1
        fi
        
        # Java environment (maven-deps'ten geliyor)
        export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
        export PATH=$$JAVA_HOME/bin:$$PATH
        export MAVEN_OPTS="-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication"
        
        if [ "${_ENVIRONMENT}" = "prod" ]; then
          echo "🔍 Production testleri - tüm testler..."
          ./mvnw -B test -Dmaven.repo.local=/workspace/.m2/repository -q
        else
          echo "⚡ Development testleri..."
          ./mvnw -B test -Dtest=!*IntegrationTest -Dmaven.test.failure.ignore=true -Dmaven.repo.local=/workspace/.m2/repository -q
        fi
        echo "✅ Testler başarılı!"
    waitFor: ['maven-deps']

  # 3. Build aşaması (parallel with test)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'build'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "🔨 Backend build ediliyor..."
        echo "Mevcut dizin: $(pwd)"
        # Root dizinden hss-backend'e geç veya mevcut dizinde kal
        if [ -d "hss-backend" ]; then
          cd hss-backend
          echo "hss-backend dizinine geçildi"
        elif [ -f "pom.xml" ]; then
          echo "Mevcut dizinde pom.xml bulundu, burada kalıyoruz"
        else
          echo "❌ hss-backend dizini veya pom.xml bulunamadı!"
          echo "Mevcut dizin içeriği:"
          ls -la
          exit 1
        fi
        
        # Java environment (maven-deps'ten geliyor)
        export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
        export PATH=$$JAVA_HOME/bin:$$PATH
        export MAVEN_OPTS="-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication"
        
        ./mvnw -B clean package -DskipTests -Dmaven.repo.local=/workspace/.m2/repository -q
        echo "✅ Build başarılı!"
    waitFor: ['maven-deps']

  # 4. Docker build (optimized caching)
  - name: 'gcr.io/cloud-builders/docker'
    id: 'docker-build'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        MY_IMG_PATH="${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend"

        echo "🐳 Docker image build başlıyor..."
        echo "Environment: ${_ENVIRONMENT}, Tag: ${_TAG_NAME}"
        echo "Mevcut dizin: $(pwd)"
        echo "Dizin içeriği: $(ls -la)"

        # Root dizinden hss-backend'e geç veya mevcut dizinde kal
        if [ -d "hss-backend" ]; then
          cd hss-backend
          echo "hss-backend dizinine geçildi"
        elif [ -f "pom.xml" ]; then
          echo "Mevcut dizinde pom.xml bulundu, burada kalıyoruz"
        else
          echo "❌ hss-backend dizini veya pom.xml bulunamadı!"
          echo "Mevcut dizin içeriği:"
          ls -la
          exit 1
        fi

        # Multiple cache sources for better hit rate
        docker pull "$${MY_IMG_PATH}:latest" || echo "No latest image found"
        docker pull "$${MY_IMG_PATH}:${_TAG_NAME}" || echo "No tagged image found"

        # Build with optimized cache strategy
        docker build \
          --tag "$${MY_IMG_PATH}:${SHORT_SHA}" \
          --tag "$${MY_IMG_PATH}:${_TAG_NAME}" \
          --tag "$${MY_IMG_PATH}:latest" \
          --cache-from "$${MY_IMG_PATH}:latest" \
          --cache-from "$${MY_IMG_PATH}:${_TAG_NAME}" \
          --build-arg BUILDKIT_INLINE_CACHE=1 \
          --build-arg BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ") \
          --build-arg VCS_REF=${SHORT_SHA} \
          --build-arg ENVIRONMENT=${_ENVIRONMENT} \
          --progress=plain \
          --memory=2g \
          --memory-swap=2g \
          .

        echo "✅ Docker build başarılı!"
    waitFor: ['test', 'build']

  # 5. Docker push
  - name: 'gcr.io/cloud-builders/docker'
    id: 'docker-push'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        MY_IMG_PATH="${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend"
        echo "📤 Docker image push başlıyor..."
        docker push "$${MY_IMG_PATH}:${SHORT_SHA}"
        docker push "$${MY_IMG_PATH}:${_TAG_NAME}"
        docker push "$${MY_IMG_PATH}:latest"
        echo "✅ Docker push tamamlandı!"
    waitFor: ['docker-build']

  # 6. Database Migration (sadece prod için)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'migrate'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -euo pipefail
        if [ "${_ENVIRONMENT}" = "prod" ]; then
          echo "🗄️ Production database migration başlıyor..."
          if [ -d "hss-backend" ]; then
            cd hss-backend
          fi
          
          # Java environment (maven-deps'ten geliyor)
          export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
          export PATH=$$JAVA_HOME/bin:$$PATH
          export MAVEN_OPTS="-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication"
          
          ./mvnw -B flyway:migrate \
            -Dflyway.url="jdbc:postgresql:///hss_prod?socketFactory=com.google.cloud.sql.postgres.SocketFactory&cloudSqlInstance=${_CONNECTION_NAME}" \
            -Dflyway.user="${_DB_USERNAME_PROD_SECRET}" \
            -Dflyway.password="${_DB_PASSWORD_PROD_SECRET}" \
            -Dflyway.locations=classpath:db/migration \
            -Dflyway.baseline-on-migrate=true \
            -q
          
          echo "✅ Database migration tamamlandı!"
        else
          echo "ℹ️ Dev ortamı - migration atlandı"
        fi
    waitFor: ['docker-push']

  # 7. Cloud Run deploy
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -euo pipefail

        echo "🚀 Cloud Run deploy başlıyor..."
        echo "Environment: ${_ENVIRONMENT}"

        if [ "${_ENVIRONMENT}" = "prod" ]; then
          MY_SPRING_PROFILE="prod"
          MY_SERVICE_NAME="hss-backend"
          MY_MIN_INSTANCES="1"
          MY_MAX_INSTANCES="10"
          MY_MEMORY="1Gi"
          MY_CPU="2"
          MY_TIMEOUT="600"
          MY_CONCURRENCY="100"
          MY_DB="hss_prod"
          MY_DB_USERNAME_SECRET="${_DB_USERNAME_PROD_SECRET}"
          MY_DB_PASSWORD_SECRET="${_DB_PASSWORD_PROD_SECRET}"
        else
          MY_SPRING_PROFILE="dev"
          MY_SERVICE_NAME="hss-backend-dev"
          MY_MIN_INSTANCES="0"
          MY_MAX_INSTANCES="3"
          MY_MEMORY="512Mi"
          MY_CPU="1"
          MY_TIMEOUT="300"
          MY_CONCURRENCY="80"
          MY_DB="hss_dev"
          MY_DB_USERNAME_SECRET="${_DB_USERNAME_SECRET}"
          MY_DB_PASSWORD_SECRET="${_DB_PASSWORD_SECRET}"
        fi

        gcloud run deploy "$${MY_SERVICE_NAME}" \
          --image="${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend:${SHORT_SHA}" \
          --region="${_REGION}" \
          --service-account="${_SERVICE_ACCOUNT}" \
          --platform=managed \
          --allow-unauthenticated \
          --set-env-vars="SPRING_PROFILES_ACTIVE=$${MY_SPRING_PROFILE},DB_NAME=$${MY_DB}" \
          --set-env-vars="GCP_SQL_INSTANCE_CONNECTION_NAME=${_CONNECTION_NAME}" \
          --set-secrets="DB_USERNAME=$${MY_DB_USERNAME_SECRET}:latest,DB_PASSWORD=$${MY_DB_PASSWORD_SECRET}:latest" \
          --add-cloudsql-instances="${_CONNECTION_NAME}" \
          --min-instances="$${MY_MIN_INSTANCES}" \
          --max-instances="$${MY_MAX_INSTANCES}" \
          --memory="$${MY_MEMORY}" \
          --cpu="$${MY_CPU}" \
          --timeout="$${MY_TIMEOUT}" \
          --concurrency="$${MY_CONCURRENCY}" \
          --port=8080 \
          --no-traffic \
          --quiet

        MY_URL=$(gcloud run services describe "$${MY_SERVICE_NAME}" --region=${_REGION} --format='value(status.url)')
        echo "✅ Deploy başarılı: $${MY_SERVICE_NAME}"
        echo "🌐 URL: $${MY_URL}"

        # Optimized health check with retry logic
        echo "🔍 Health check başlıyor..."
        for i in {1..5}; do
          if curl -sf "$${MY_URL}/actuator/health" >/dev/null 2>&1; then
            echo "✅ Servis sağlıklı! (deneme $i/5)"
            break
          else
            echo "⏳ Servis başlıyor... (deneme $i/5)"
            sleep 10
          fi
        done
    waitFor: ['migrate']

  # 8. Release artifacts (only prod)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'release'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        if [ "${_ENVIRONMENT}" = "prod" ]; then
          echo "📦 Release artifacts oluşturuluyor..."
          MY_IMG_PATH="${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend:${SHORT_SHA}"
          MY_VERSION="${_TAG_NAME}"
          MY_BUCKET="gs://hss-releases-${PROJECT_ID}"

          cat > manifest.json << EOF
        {
          "version": "$${MY_VERSION}",
          "environment": "production",
          "image": "$${MY_IMG_PATH}",
          "deployed_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        }
        EOF
          gsutil -m cp manifest.json "$${MY_BUCKET}/releases/$${MY_VERSION}/"
          gsutil -m cp manifest.json "$${MY_BUCKET}/releases/latest/manifest.json"
          echo "✅ Release artifacts yüklendi!"
        else
          echo "ℹ️ Dev ortamı - release atlandı"
        fi
    waitFor: ['deploy']

  # 9. Monitoring metrics (parallel with release)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'monitoring'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "📊 Monitoring kaydı gönderiliyor..."
        gcloud logging write hss-deployment \
          "Backend deployment completed successfully" \
          --severity=INFO \
          --payload-type=json \
          --payload='{
            "service": "hss-backend",
            "environment": "${_ENVIRONMENT}",
            "version": "${_TAG_NAME}",
            "commit_sha": "${SHORT_SHA}",
            "deployment_time": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
          }' || echo "⚠️ Monitoring gönderilemedi"
    waitFor: ['deploy']

# Substitution variables
substitutions:
  _REGION: 'europe-west3'
  _REPO_NAME: 'hss-backend'
  _SERVICE_ACCOUNT: 'hss-backend-sa@hss-cloud-473511.iam.gserviceaccount.com'
  _CONNECTION_NAME: 'hss-cloud-473511:europe-west3:hss-sql'
  _DB_USERNAME_SECRET: 'DB_USERNAME'
  _DB_PASSWORD_SECRET: 'DB_PASSWORD'
  _DB_USERNAME_PROD_SECRET: 'DB_USERNAME_PROD'
  _DB_PASSWORD_PROD_SECRET: 'DB_PASSWORD_PROD'
  _ENVIRONMENT: 'dev'
  _TAG_NAME: 'dev'

# Options (optimized for performance)
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'  # 8 vCPU, 8GB RAM - desteklenen ve ekonomik
  diskSizeGb: 50  # Daha küçük disk
  substitution_option: 'ALLOW_LOOSE'
  dynamic_substitutions: false  # JAVA_HOME gibi bash değişkenleri için disabled
  env:
    - 'DOCKER_BUILDKIT=1'
    - 'MAVEN_OPTS=-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication'  # Optimized Maven memory
    - 'GRADLE_OPTS=-Xmx512m -XX:+UseG1GC -XX:+UseStringDeduplication'

timeout: '900s'  # 15 dakika - daha kısa timeout

images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend:${SHORT_SHA}'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend:${_TAG_NAME}'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/hss-backend:latest'